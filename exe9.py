prod = float (input(" Qual é o preço do produto ? "))

des = int (input(" Qual é o desconto ? "))

desconto = prod - des

print(" O produto custava " + str(prod) + " mas com o desconto ele custa " + str(desconto))